# tasik-screen-recorder
a very high quality screen recorder developed to run as a browser extension
put all files in a single folder no external file needed. 
load the file as unpacked your favourite browser.
#search your browser dev on how to install unpacked extensions
